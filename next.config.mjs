/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    API_URL: "https://cs-api.mainulhasan05.xyz",
    // API_URL: "http://localhost:5000",
    SITE_URL: "https://codesharer.xyz/",
  },
};

export default nextConfig;
